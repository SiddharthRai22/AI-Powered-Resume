# AI Resume Builder — Frontend Environment Variables

# Backend API base URL (used only when NOT running through Vite proxy)
# When running locally with Vite, the proxy in vite.config.js handles /api routing.
# Leave this empty for local development.
VITE_API_URL=
